# R14 recovery candidate

This generated package advances current Results, both official standings, driver statistics, Results Archive, Stats Core, base Stats and catalogue completion to Spanish Round 14 on 2026-09-13. All 23 driver and 11 constructor positions/points match two stable official observations. Current response shapes and canonical IDs are unchanged.

`f1/current/v1/manifest.json` binds seven exact current response files to immutable payload paths. Its archive manifest digest is `fa294210140a10f2f2f3940effd2fb5db8d809d0560f709384e1838f67cedabf`. Repeated generation is a no-op. An existing content address cannot be overwritten with different bytes.

Previously approved profile/circuit facts remain dated 2026-09-06. Their exact verified source inputs are retained in `f1/profiles/input-snapshots/`; no old fact is relabelled as current. Driver numeric statistics are available through the current snapshot. Team profile/circuit refresh needs separate reviewed source evidence. CC0-dependent backfill retains its 2026-08-23 scope. The previously generated shared-driver correction contains 1,864 independently reconciled groups and still requires operational publication.

`docs/recovery-evidence/catalogue-numeric-audit.json` lists all 169 additional metrics: 168 populated metrics, 65,874 output values checked with separate arithmetic, one source-unavailable metric. Shared source normalization, complete eligible population and age-event selection are explicitly outside that independent audit. The four home-GP metrics and average driver age remain unavailable for missing historical nationality/DOB evidence.

Value R14 has been recalculated and repeated without changes in a separate candidate; the approved public pointer is preserved pending review of the changed producer fingerprint. Approved Power pointers are preserved. New strength research has 98/260 qualifying coverage, below the unchanged 50% gate, and is never a production source. The new evaluation retains the verified R13 race seed; R14 preparation requires canonical race-seed replay before it can proceed.

For workflow failures, official source URLs, the complete producer/consumer map, exact exclusion reasons and reproduction, read `docs/post-session-recovery-20260914.md` in entrance-f1-data-tools. Local generation, manual verification CI, scheduled execution and public activation must be recorded separately. This document does not authorize main integration or deployment.
