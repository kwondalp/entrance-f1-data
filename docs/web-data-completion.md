# Verified Web completion package

Constructor career points and entries are bound to the official Team Summary
snapshot after checking its season coverage against the current result fixture.
Current-season classification corrections do not imply the same delta for a
historical constructor lineage total.

This feature candidate is generated by the paired Tools completion change.
Producer dependency: `entrance-f1-data-tools` commit
`4a874f2df0af2753b09b7e223a5e13757c29d299` on
`codex/web-data-completion-20260906` (including completion commit `4e2f2a3`).
This producer also preserves pinned historical replay and keeps older optional
profile evidence separate from a newly verified race correction. All 914 Tools
tests passed with both candidate Data environment variables set. These final
producer guards reproduce this current Data package without further value changes.
`f1/profiles/v1/manifest.json` binds 23 driver, 11 team and 23 circuit records.
`f1/stats-catalogue-completion/v1/manifest.json` binds 169 supplementary definitions:
168 have rows; whole-field average driver age remains empty with explicit exclusions.
The catalogue includes one replacement for existing driver fastest-lap totals.

The archive still ends at the Dutch GP, 2026-08-23. Fresh official Monaco evidence
changes Hadjar, Piastri, Lawson, Lindblad and Gasly results/aggregates and the
related constructor season standings. Current season, Archive v2, Stats Lab,
Core and backfill manifests are regenerated coherently. The historical approved
fixture and prior producer descriptors remain immutable in Tools.

Current seats and season participation differ; Tsunoda’s reserve affiliation is
Red Bull while his season participation is Racing Bulls. Team official profile
career totals describe their published lineage, not a merger of canonical
historical chassis identities. Circuit lap records retain both time and holder;
Madrid’s not-yet-established record is null, not zero.

Sources and retrieval hashes are inside the profile package and paired Tools
fixtures. Historical F1DB material is CC BY 4.0, Marcel Overdijk and contributors,
v2026.11.0 through the Hungarian GP. The supplement also binds separately accepted
birthdate, pole and complete CC0 lap-chart inputs. Per-metric coverage and exclusions
remain part of the contract; a nonempty partial metric is not an all-time total.

Four home-GP metrics still lack event-effective historical nationality. Whole-field
average age requires the existing separate human birthdate review. These five
routes remain unresolved; eleven future race rows omitted by Web are not missing
published data. Existing provisional historical constructor identities are unchanged.

No production automation or deployment was activated. The paired Tools workflow
prepares source detection, validation and feature candidates; main promotion and
activation remain separate owner actions. See the paired Tools
`docs/web-data-completion/audit.json` for the full route/field audit and the paired
Web `docs/data-completion/` review for screenshots and exact commit dependencies.
